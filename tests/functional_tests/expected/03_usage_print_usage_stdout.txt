usage: tcmReceiver print [-h]

TcmReceiver 'print' command
===========================

Capture from the serial port (or input file) and parse the data to print it
on stdout.

Example:
    tcmReceiver -d /dev/ttyUSB1 print

optional arguments:
  -h, --help  show this help message and exit
